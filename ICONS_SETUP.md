# Настройка иконок папок

## Проблема с путями к иконкам

Если иконки папок не отображаются (показывается эмодзи 📁), это означает, что иконки не опубликованы или путь к ним неправильный.

## Решение

### Шаг 1: Опубликуйте иконки папок

```bash
php artisan vendor:publish --tag=media-assets
```

Это скопирует иконки в `public/img/system/media/`.

### Шаг 2: Проверьте структуру папок

После публикации должна быть следующая структура:

```
public/
  img/
    system/
      media/
        folder.png
        basket.png
        folder-protected.png
        ... (другие иконки)
```

### Шаг 3: Проверка в браузере

Откройте в браузере: `http://your-domain/img/system/media/folder.png`

Если изображение загружается - путь правильный. Если нет - проверьте права доступа к папке `public/img/system/media/`.

## Альтернативные пути

Компонент автоматически пробует следующие пути (в порядке приоритета):

1. `/img/system/media/folder.png` - стандартный путь после публикации
2. `/img/system/folder.png` - альтернативный путь
3. `/system/folder.png` - старый путь
4. Эмодзи 📁 - fallback, если ни один путь не работает

## Для конкретных папок

Если папка имеет поле `src` (например, `src: 'folder-protected'`), компонент будет искать:

1. `/img/system/media/folder-protected.png`
2. `/img/system/folder-protected.png`
3. `/system/folder-protected.png`
4. Эмодзи 📁 - fallback

## Ручная настройка путей

Если вы хотите использовать другой путь для иконок, отредактируйте файл после публикации:

`resources/js/vendor/media/components/Media.vue`

Найдите функцию `getDefaultFolderIcon()` и `getFolderIcon()` и измените пути на свои.

## Проверка после исправления

После исправления:
1. Пересоберите фронтенд: `npm run build`
2. Очистите кеш браузера (Ctrl+Shift+R)
3. Проверьте консоль браузера на наличие ошибок загрузки изображений

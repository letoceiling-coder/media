# Чек-лист установки пакета letoceiling-coder/media

Используйте этот чек-лист для проверки корректной установки пакета.

## ✅ Автоматическая установка

```bash
composer require letoceiling-coder/media
php artisan media:install
php artisan migrate
npm install vue@^3.5.0 vue-router@^4.6.0 fslightbox-vue@^3.0.1 sweetalert2@^11.26.3 vue-advanced-cropper@^2.8.9
```

## ✅ Проверка установки

### 1. Файлы опубликованы

- [ ] Миграции в `database/migrations/`
- [ ] Конфигурация в `config/media.php` (опционально)
- [ ] Vue компоненты в `resources/js/vendor/media/`
  - [ ] `components/Media.vue`
  - [ ] `components/EditImage.vue`
  - [ ] `utils/api.js`
  - [ ] `composables/useAuthToken.js`
- [ ] CSS стили в `resources/css/vendor/media.css`
- [ ] Иконки в `public/img/system/media/`
  - [ ] `folder.png`
  - [ ] `basket.png`
  - [ ] `document.png`
  - [ ] `video.png`
  - [ ] `music.png`
  - [ ] `no-image.png`

### 2. CSS подключен

- [ ] В `resources/css/app.css` есть строка: `@import './vendor/media.css';`
- [ ] Файл `resources/css/vendor/media.css` существует

### 3. Роут для редактирования добавлен

- [ ] В файле роутов (например, `resources/js/router/admin.js`) добавлен роут:
  ```javascript
  {
    path: 'media/:id/edit',
    name: 'admin.media.edit',
    component: () => import('@/vendor/media/components/EditImage.vue'),
    meta: { title: 'Редактировать изображение' },
  }
  ```

### 4. JavaScript зависимости установлены

- [ ] `vue@^3.5.0`
- [ ] `vue-router@^4.6.0`
- [ ] `fslightbox-vue@^3.0.1`
- [ ] `sweetalert2@^11.26.3`
- [ ] `vue-advanced-cropper@^2.8.9`

### 5. Миграции выполнены

- [ ] Выполнено: `php artisan migrate`
- [ ] Таблицы `folders` и `media` созданы в БД

### 6. Фронтенд пересобран

- [ ] Выполнено: `npm run build` или `npm run dev`
- [ ] Нет ошибок при сборке

## 🔍 Проверка работоспособности

### API роуты работают

- [ ] `GET /api/v1/folders` - возвращает список папок
- [ ] `GET /api/v1/media` - возвращает список файлов
- [ ] API требует авторизацию (возвращает 401 без токена)

### Компонент отображается

- [ ] Страница `/admin/media` открывается без ошибок
- [ ] Папки отображаются с иконками (не эмодзи 📁)
- [ ] Файлы отображаются корректно
- [ ] Загрузка файлов работает

### Редактирование изображений работает

- [ ] При клике на кнопку редактирования (✏️) происходит переход на `/admin/media/{id}/edit`
- [ ] Страница редактирования открывается с редактором изображений
- [ ] Редактор позволяет обрезать, поворачивать, отражать изображение
- [ ] Сохранение изображения работает

## ❌ Типичные проблемы

### Проблема: Иконки папок не отображаются

**Решение:**
```bash
php artisan vendor:publish --tag=media-assets --force
```

### Проблема: Ошибка "No match for route admin.media.edit"

**Решение:** Добавьте роут в ваш Vue Router (см. пункт 3 выше).

### Проблема: Компонент отображается без стилей

**Решение:**
1. Проверьте подключение CSS: `@import './vendor/media.css';` в `app.css`
2. Пересоберите фронтенд: `npm run build`

### Проблема: Ошибки в консоли браузера

**Решение:** Убедитесь, что все npm зависимости установлены:
```bash
npm install vue@^3.5.0 vue-router@^4.6.0 fslightbox-vue@^3.0.1 sweetalert2@^11.26.3 vue-advanced-cropper@^2.8.9
```

## 📞 Поддержка

Если проблемы не решаются, создайте issue в репозитории:
https://github.com/letoceiling-coder/media/issues


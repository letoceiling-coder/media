# Быстрая установка пакета letoceiling-coder/media

## 🚀 Автоматическая установка (3 команды)

```bash
composer require letoceiling-coder/media
php artisan media:install
php artisan migrate
npm install vue@^3.5.0 vue-router@^4.6.0 fslightbox-vue@^3.0.1 sweetalert2@^11.26.3 vue-advanced-cropper@^2.8.9
npm run build
```

**Готово!** Пакет установлен и настроен.

## Что делает команда `php artisan media:install`?

Команда автоматически:
- ✅ Публикует миграции в `database/migrations/`
- ✅ Публикует конфигурацию в `config/media.php`
- ✅ Публикует Vue компоненты в `resources/js/vendor/media/`
- ✅ Публикует CSS стили в `resources/css/vendor/media.css`
- ✅ Публикует системные иконки в `public/img/system/media/`
- ✅ Проверяет подключение CSS стилей
- ✅ Показывает следующие шаги (миграции, npm, роуты)

## Дополнительные шаги

После установки не забудьте:

1. **⚠️ ВАЖНО: Добавить роут для редактирования изображений** в ваш Vue Router (файл `resources/js/router/admin.js` или аналогичный):

```javascript
{
  path: 'media/:id/edit',
  name: 'admin.media.edit',
  component: () => import('@/vendor/media/components/EditImage.vue'),
  meta: { title: 'Редактировать изображение' },
}
```

**Без этого роута функция редактирования фото не будет работать!**

2. **Подключить CSS стили** (если не подключены автоматически) в `resources/css/app.css`:
```css
@import './vendor/media.css';
```

3. **Пересобрать фронтенд**:
```bash
npm run build
```

## Опции команды установки

```bash
# Установка с перезаписью существующих файлов
php artisan media:install --force

# Установка только API (без Vue компонентов)
php artisan media:install --no-components --no-styles --no-assets
```

## Документация

- [README.md](README.md) - Основная документация
- [INSTALLATION_STEPS.md](INSTALLATION_STEPS.md) - Подробная инструкция
- [VUE_COMPONENTS.md](VUE_COMPONENTS.md) - Использование Vue компонентов

